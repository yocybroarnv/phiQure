# Template for GitHub Actions/CI secrets
# Copy this file to .env or your CI/CD secrets manager

# Backend
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=phiqure
SECRET_KEY=changeme-in-prod
ALLOWED_HOSTS=*

# Frontend
REACT_APP_API_URL=http://localhost:8000/api/v1

# Docker/Registry (for image push)
DOCKERHUB_USERNAME=
DOCKERHUB_TOKEN=
IMAGE_TAG=latest
